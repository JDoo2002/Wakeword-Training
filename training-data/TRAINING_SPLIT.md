# wake word recording split

- Training: `wakeword_001.wav` through `wakeword_020.wav`
- Held-out test: `wakeword_021.wav` through `wakeword_030.wav`

The held-out files must not be uploaded for training. They are reserved for measuring whether personalization generalizes to recordings the model has not seen.
